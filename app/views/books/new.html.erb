<% if current_user.admin? %>
<div class="container">
<h1>New Book</h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to "Back to Books", books_path, class: "btn btn-secondary" %>
  
</div>
</div>
<%else %>
<center>
<h5> Only an admin can add a Book</h5>
<%= link_to "Back to Books", books_path, class: "btn btn-secondary" %>
</center>
<% end %>
