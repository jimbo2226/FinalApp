<% if current_user %>
<div id="<%= dom_id book %>">

   <h2><strong>Title:</Strong> <%= book.title %></h2>
   <h5><strong>Posted by:</Strong> <%= link_to book.user.name, user_path(book.user) %></h5>
 <h5><strong>Author:</Strong> <%= book.author %><h5>
  <div>
   
   <h5><strong>Description:</Strong><%= book.discription %></h5>
    </div>

    <h6><%= pluralize(book.views, "view") %></h6>

</div>
<% end %>
